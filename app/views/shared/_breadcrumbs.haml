%ul.breadcrumb
  %li
    = link_to root_url do
      %span.icon.icon-home
      = t(:'site.nav.home')
    %span.divider &rarr;
  - if breadcrumb_items
    - for item in breadcrumb_items do
      %li= link_to item, item
