.fourcol
  -#= render :partial => 'todo_lists/form', :locals => { :todo_list => TodoList.new }
  %h2{ :title => 'You may invite other users to your projects or be invited to theirs'} Your projects
  %ul{ :id => 'projects' }
    = render projects
  = link_to new_project_path, :class => 'link-button' do
    %span.icon.icon-plus-circle
    Add new project
.fourcol
  %h2{ :title => 'Only you can see and manage your private todo lists' } Your private todo lists
  -#= render :partial => 'todo_lists/form', :locals => { :todo_list => TodoList.new }
  - if todo_lists.any?
    %ul#todo-lists
      = render todo_lists
  - else
    .notice
      =t :'todo_lists.messages.no_todo_lists_added'
  = link_to new_todo_list_path, :class => 'link-button' do
    %span.icon.icon-document-plus
    Add new todo list
.fourcol.last
  %h2 Your events
  -if current_user.invitations.any?
    %h3 You've been invited to these projects:
    %ul#invitations
    - for invitation in current_user.invitations do
      %li
        %span.icon.icon-block-small
        %strong= invitation.project
        by
        %em= invitation.inviter
        %br/
        = link_to 'Accept', accept_collaborator_path( invitation)
